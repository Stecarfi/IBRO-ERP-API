const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const data = { id: Date.now().toString(), name: 'Prisma Test Role', permissions: '{}' }; const res = await prisma.role.upsert({ where: { id: data.id }, update: data, create: data }); console.log('Success:', res); } catch (err) { console.error('Error:', err); } finally { await prisma.$disconnect(); } } main();
