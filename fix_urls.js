const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany(); let count = 0; for (const u of users) { if (u.foto && u.foto.includes("http://ibro-api.onrender.com")) { await prisma.user.update({ where: { id: u.id }, data: { foto: u.foto.replace("http://", "https://") } }); count++; } } console.log("Fixed " + count + " users"); } main().catch(console.error).finally(() => prisma.$disconnect());
